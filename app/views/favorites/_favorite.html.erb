<% if user_signed_in? && post.liked_by?(current_user)%>
  <%=link_to "いいね済", post_favorites_path(post.id),  method: :delete, class: "post_favorites", remote: true %>
<% else %>
  <%=link_to "いいねする", post_favorites_path(post.id), method: :post, class: "post_favorites", remote: true %>
<% end %>